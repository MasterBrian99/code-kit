function App() {
  //   const updateResultText = (result: string) => setResultText(result);

  //   function greet() {
  //     Greet(name).then(updateResultText);
  //   }

  return (
    <div id="App">
      <h1>as</h1>
    </div>
  );
}

export default App;
